{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug bin/app",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/bin/app",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "make: debug"
    },
    {
      "name": "Debug bin/app with args",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/bin/app",
      "args": ["arg1", "arg2"],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "make: debug"
    }
  ]
}
